# Cuestionario Semana 03: Visualizacion y Matplotlib basico

## Pregunta 1

Que libreria se utiliza principalmente para crear graficos estaticos en Python?

a. NumPy
b. Pandas
c. Matplotlib
d. Requests

> solucion: C

## Pregunta 2

Que instruccion importa el modulo principal de Matplotlib para graficar?

a. `import matplotlib.pyplot as plt`
b. `import matplotlib as pd`
c. `import pyplot from matplotlib`
d. `import plt.matplotlib`

> solucion: A

## Pregunta 3

Que tipo de grafico es mas adecuado para mostrar la evolucion de ventas por mes?

a. Grafico de linea
b. Grafico de torta
c. Boxplot
d. Heatmap

> solucion: A

## Pregunta 4

Que funcion de Matplotlib permite crear un grafico de barras?

a. `plt.scatter()`
b. `plt.bar()`
c. `plt.hist()`
d. `plt.boxplot()`

> solucion: B

## Pregunta 5

Que grafico se recomienda para observar la distribucion de una variable numerica?

a. Histograma
b. Grafico de linea
c. Grafico de barras agrupadas
d. Diagrama de red

> solucion: A

## Pregunta 6

Que funcion permite crear un grafico de dispersion?

a. `plt.plot()`
b. `plt.scatter()`
c. `plt.imshow()`
d. `plt.table()`

> solucion: B

## Pregunta 7

En la API orientada a objetos de Matplotlib, que representan `fig` y `ax`?

a. `fig` es el eje x y `ax` es el eje y
b. `fig` es la figura completa y `ax` es el area donde se dibuja el grafico
c. `fig` es una tabla y `ax` es una columna
d. `fig` es el color y `ax` es la leyenda

> solucion: B

## Pregunta 8

Que metodo se usa para agregar un titulo a un grafico cuando se trabaja con `ax`?

a. `ax.title()`
b. `ax.set_title()`
c. `ax.name_title()`
d. `ax.add_title()`

> solucion: B

## Pregunta 9

Que grafico es util para comparar la distribucion de una variable entre grupos y detectar valores extremos?

a. Boxplot
b. Grafico de linea
c. Grafico de barras simples
d. Grafico de area

> solucion: A

## Pregunta 10

Que funcion se utiliza normalmente para mostrar el grafico al final de una celda?

a. `plt.open()`
b. `plt.render()`
c. `plt.show()`
d. `plt.run()`

> solucion: C
